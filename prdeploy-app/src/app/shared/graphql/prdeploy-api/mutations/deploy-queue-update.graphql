mutation DeployQueueUpdate($owner: ID!, $repo: ID!, $environment: ID!, $pullRequestNumbers: [ID!]) {
  deployQueueUpdate(owner: $owner, repo: $repo, environment: $environment, pullRequestNumbers: $pullRequestNumbers) {
    environment
  }
}
